package rol

extern fun toString(x: dyn?): String = """
    if x == nil then
        return "null"
    else
        return tostring(x)
    end
"""

extern fun toNumber(x: String): Number = "return tonumber(x)"